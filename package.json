{
    "name": "vittara-ai",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "install": "npm --prefix client install",
        "build": "npm --prefix client run build && node -e \"require('fs').cpSync('client/dist', 'dist', {recursive: true})\""
    },
    "devDependencies": {
        "vite": "^6.1.0"
    }
}